Original prompt: complete the new game , add this also with other three option

- 2026-08-21: Replaced Jigsaw Mission's manual per-piece unlock selectors with an automatic balanced schedule. Examples verified: 10 questions / 5 pieces = 2 each; 10 / 4 = 3, 2, 3, 2; 12 / 4 shows one-third progress after one correct answer.
- 2026-08-21: Added a locked current-piece preview with a grayscale base and proportional color overlay. Only 100%-colored pieces enter the usable/rotatable collection; partial progress is derived from existing correct-answer state and needs no new database writes or migration.
- 2026-08-21: Live Supabase-backed browser verification passed for an 8-question / 4-piece mission: the first piece rendered 0% grayscale, became 50% colored after one correct answer while locked, then entered the rotatable collection only after the second correct answer. The next piece reset to 0%; no console errors occurred.
- 2026-08-21: Removed the manual author schedule controls and verified the 10-question / 4-piece setup displays the automatic 3, 2, 3, 2 distribution. Jigsaw Mission now permits up to 20 questions.
- 2026-08-21: Final verification passes: frontend lint, TypeScript, production build, backend checks, calculation assertions, author UI Playwright, and the full 0% -> 50% locked -> 100% usable gameplay sequence. Disposable Supabase room and browser artifacts were deleted.
- 2026-08-21: Began per-question timing across all current modes. Added a backward-compatible `timerMode` payload setting (`overall` or `per_question`), mode-aware presets/validation/instructions, author controls, and persistence serialization.
- 2026-08-21: Added authoritative participant question timers in `gamibar_attempts.payload`; whole-game timing still uses `room.endsAt`, while per-question mode stores only one deadline per participant and uses Supabase Realtime Broadcast solely for invalidation.
- 2026-08-21: Added backend timeout progression for quiz, Target Hunt, both jigsaw question flows, Polls, and Connect Dots pairs, plus one-question-at-a-time Poll play and client countdown reset plumbing.
- 2026-08-21: Verified the timer selector in an authenticated Playwright run: `Each question` selected, a fresh 30-second countdown rendered, and no console/page errors were reported.
- 2026-08-21: Verified authoritative deadline reset/expiry against the configured live Supabase project for all six modes. Disposable smoke rooms were deleted afterward; no database migration was required because timer state uses the existing attempt JSON payload.
- 2026-08-21: Final checks pass: frontend TypeScript, frontend production build, and backend TypeScript/JavaScript checks. Also fixed two narrow route/URL typing leftovers found by the full frontend typecheck.

2026-08-18

- Inspected the pasted Target Hunt spec and the GamiBAR room architecture.
- Existing live modes use `gamibar_rooms.config` for trusted author payloads, `toPublicGamePayload` for student-safe snapshots, `/api/game/*` for answer submission, and `gamibar_attempts` for progress/leaderboard.
- Plan: add `visual_point` as a core live mode, upload Target Hunt images to dedicated Supabase storage, hide `isCorrect`/`adminReference` in student payloads, and validate selected point IDs server-side.
- Implemented Target Hunt authoring, student play, server validation, scoring, persistence, Supabase migration/types, and public/author mode-picker entries.
- Verified `npm.cmd run build`, backend `npm.cmd run check`, HTTP 200 for `http://127.0.0.1:8082/author/create?mode=visual_point`, and a Playwright screenshot of `/games` showing Target Hunt as the fourth game card and footer activity.

2026-08-20

- Current request: remove TanStack/SSR and obsolete code, adopt a conventional React/Vite entry and named routes, route database work through Express, and use Supabase Realtime with a backend architecture suitable for horizontal scaling.
- Replaced hydration with `createRoot`, React Router, `index.html`, named page modules, and client-only providers.
- Moved shared game-domain code to `shared/game`, moved persistence into `backend/src/game`, and replaced frontend database reads/writes with Express APIs.
- Switched room invalidation from Postgres Changes to Supabase Broadcast, with host/public topics, burst coalescing, throttled client refreshes, and reconciliation polling.
- Added atomic event append migration, dirty-row persistence, a cached backend Supabase client, response compression, in-flight backpressure, request timeouts, and graceful shutdown.
- Removed 53 unreachable source files, 71 unused packages, old TanStack route/build files, and stale links to deleted routes.
- Verification so far: frontend lint and TypeScript pass, backend checks pass, production build passes, health returns 200, database-backed jigsaw categories returns 10 rows, and SPA deep links return the application shell.
- Remaining verification: run the Playwright client, inspect its screenshot, and record any deployment-only migration/plan requirements.
- Playwright browser verification completed for `/`, `/games`, and the guarded `/author/create` deep link. All three screenshots were visually inspected, the auth redirect rendered correctly, and the browser reported no console or page errors.
- Deployment TODO: apply `backend/supabase/migrations/20260820174442_atomic_room_event_append.sql` to the connected Supabase project before relying on atomic event appends; the backend keeps a compatibility fallback until then.
- Capacity TODO: select a Supabase Realtime tier and horizontally scaled backend compute sized for the required concurrent connection/message rate; code-level backpressure cannot replace provider capacity.
- Added randomized student invalidation/refetch delays and 60-second jittered reconciliation to avoid synchronized reconnect or room-state traffic spikes; hosts retain fast reconciliation.
- Added `backend/SCALING.md` and Render production/backpressure/shutdown defaults without forcing a paid plan change. Final Playwright click-through from Home to Join rendered correctly with no browser errors.
- 2026-08-21: Added authenticated full-question AI authoring for Quiz Challenge, Puzzle Quest, and Jigsaw Mission. Authors provide a topic, target audience, optional guidance, and a batch size capped at 10; Express generates and validates one question at a time so the UI reports real `n/total` progress.
- 2026-08-21: AI-generated batches fill completely blank rows first, never overwrite partially authored questions, and remain editable after review/apply. Single-question option generation now preserves every teacher-entered option and asks the provider to generate only missing choices.
- 2026-08-21: Verified live provider generation for a complete Grade 5 question and a partial-option case, server rejection of an 11-question request, unauthenticated endpoint rejection with HTTP 401, frontend typecheck/lint/build, backend checks, and browser flows for `1/3` progress, review/apply, and partial-option preservation with no console/page errors.
- 2026-08-21: Production hardening moved the Render Blueprint to the repository root, added deploy-time checks/security headers/CORS/readiness/backpressure/cold-start handling, completed password recovery, and aligned the local production environment URLs.
- 2026-08-21: Supabase Realtime now uses private read-only Broadcast channels. A live end-to-end check proved anonymous subscription works, anonymous private sends are denied, backend service-role sends arrive, and public broadcasts cannot reach private subscribers. Unused Postgres Changes publication entries were removed.
- 2026-08-21: Production browser smoke tests for Join, Pricing, Author Login, and the invalid/expired Password Recovery state rendered correctly; screenshots were inspected and there were no console/page errors. Backend checks, frontend TypeScript, production build, and dependency audits pass.
- Deployment TODO: sync the repository-root `render.yaml` to the existing Render services (or add the `/*` to `/index.html` rewrite manually) before deploying the Vite SPA; the current live site is still the old committed TanStack build.
- Platform TODO: delete the two now-inert legacy Edge Functions in the Supabase Dashboard and export the nine legacy-only room rows before removing `gamibar_live_rooms`. The local migration filenames now match all 30 live ledger versions exactly.
- 2026-08-21: Final production pass fixed fail-closed author routing/password recovery, Render free-tier cold starts, bounded AI provider failover, checked Storage deletion, private Realtime delivery retries, and visible-page Resource Drop reconciliation. Student room reconciliation is now five minutes instead of one minute.
- 2026-08-21: Applied and verified `gamibar_get_room_bundle` in live Supabase so the normalized room plus four child collections load through one service-role-only RPC. Live migration history was 31 entries at that checkpoint; the deliberately later archive migration is now `20260820232400_quarantine_legacy_live_rooms_after_release.sql`.
- 2026-08-21: Removed every runtime reference to the legacy `gamibar_live_rooms` blob and its non-atomic event fallback. Apply the quarantine migration only after the new Render API deploy passes create/join/reconnect/host-refresh smoke tests; the currently deployed legacy API still needs the public table.
- 2026-08-21: Author-uploaded puzzle and Target Hunt images are resized to a maximum 2048px WebP delivery asset and stored with long cache metadata to reduce Supabase bandwidth. Full backend checks and frontend lint/typecheck pass.
- Deployment gate: production still serves the old committed TanStack build and stale CORS because no Render MCP/API credential is connected. Commit/push the completed tree, attach the root `render.yaml` to the two existing services (cancel any duplicate-service preview), set all `sync: false` variables, and deploy before declaring the live site fixed.
- 2026-08-21: Enforced the participant business rule end to end: joining, reconnecting, playing, and public room Realtime require no account, subscription, or payment; author plans are consulted only when an author creates or manages a room. Removed the fake guest-auth marker and public demo-room creation bypass, and clarified Join, Pricing, Home, and Terms copy.
- 2026-08-21: Applied live migration `20260820232233_atomic_participant_join`. Its service-role-only `security invoker` RPCs serialize guest admission and host start on the room row, enforce the host-derived capacity, block normalized duplicate nicknames, create participant plus attempt atomically, and prevent name-based reconnect-token takeover. Parallel final-slot and join-versus-start tests passed; all temporary verification rows were removed.
- 2026-08-21: A real account-free browser flow reached the lobby through the Express backend and live Supabase, with no console/page errors. The Playwright screenshots were visually inspected. Backend checks plus frontend lint, typecheck, and production build pass.
- Supabase Dashboard TODO: disable **Realtime → Settings → Allow public access** after deployment. Account-free private participant channels will continue to work through the scoped `anon` Realtime RLS read policy.
- 2026-08-21: Raised the configurable coarse shared-IP API ceiling to 200,000 requests per 15 minutes so a campus NAT does not accidentally paywall/block a large free audience; AI, billing, uploads, concurrency, and in-flight work retain tighter independent controls. Production startup was verified healthy with every optional Razorpay/AI credential deliberately invalid, proving those author-only features cannot take free gameplay offline.
